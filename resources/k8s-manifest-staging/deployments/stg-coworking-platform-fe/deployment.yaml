apiVersion: apps/v1
kind: Deployment
metadata:
  name: stg-coworking-platform-fe
  namespace: stg
  labels:
    app: stg-coworking-platform-fe
    environment: stg
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: stg-coworking-platform-fe
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: stg-coworking-platform-fe
        environment: stg
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: stg-coworking-platform-fe
          image: 344367180480.dkr.ecr.ap-south-1.amazonaws.com/coworking-platform-fe:2880047
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: coworking-platform-fe-secret
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"

