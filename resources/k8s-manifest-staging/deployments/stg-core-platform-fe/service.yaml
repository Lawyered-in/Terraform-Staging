apiVersion: v1
kind: Service
metadata:
  name: stg-core-platform-fe
  namespace: stg
  labels:
    app: stg-core-platform-fe
    environment: stg
spec:
  type: ClusterIP
  selector:
    app: stg-core-platform-fe
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
