apiVersion: v1
kind: Service
metadata:
  name: stg-faas-be
  namespace: stg
  labels:
    app: stg-faas-be
    environment: stg
spec:
  type: ClusterIP
  selector:
    app: stg-faas-be
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
