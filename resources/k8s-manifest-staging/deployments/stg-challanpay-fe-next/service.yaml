apiVersion: v1
kind: Service
metadata:
  name: stg-challanpay-fe-next
  namespace: stg
  labels:
    app: stg-challanpay-fe-next
    environment: stg
spec:
  type: ClusterIP
  selector:
    app: stg-challanpay-fe-next
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
