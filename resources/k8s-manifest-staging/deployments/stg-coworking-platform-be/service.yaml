apiVersion: v1
kind: Service
metadata:
  name: stg-coworking-platform-be
  namespace: stg
  labels:
    app: stg-coworking-platform-be
    environment: stg
spec:
  type: ClusterIP
  selector:
    app: stg-coworking-platform-be
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
