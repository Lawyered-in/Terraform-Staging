apiVersion: v1
kind: Service
metadata:
  name: stg-partners-portal-fe
  namespace: stg
  labels:
    app: stg-partners-portal-fe
    environment: stg
spec:
  type: ClusterIP
  selector:
    app: stg-partners-portal-fe
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
