apiVersion: v1
kind: Service
metadata:
  name: stg-prosper-fe
  namespace: stg
  labels:
    app: stg-prosper-fe
    environment: stg
spec:
  type: ClusterIP
  selector:
    app: stg-prosper-fe
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
