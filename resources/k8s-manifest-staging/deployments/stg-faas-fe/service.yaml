apiVersion: v1
kind: Service
metadata:
  name: stg-faas-fe
  namespace: stg
  labels:
    app: stg-faas-fe
    environment: stg
spec:
  type: ClusterIP
  selector:
    app: stg-faas-fe
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
