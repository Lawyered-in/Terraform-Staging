apiVersion: v1
kind: Service
metadata:
  name: stg-core-platform-be
  namespace: stg
  labels:
    app: stg-core-platform-be
    environment: stg
spec:
  type: ClusterIP
  selector:
    app: stg-core-platform-be
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3333
