apiVersion: v1
kind: Service
metadata:
  name: stg-admin-lawyered
  namespace: stg
  labels:
    app: stg-admin-lawyered
    environment: stg
spec:
  type: ClusterIP
  selector:
    app: stg-admin-lawyered
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80