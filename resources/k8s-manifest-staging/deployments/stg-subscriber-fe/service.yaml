apiVersion: v1
kind: Service
metadata:
  name: stg-subscriber-fe
  namespace: stg
  labels:
    app: stg-subscriber-fe
    environment: stg
spec:
  type: ClusterIP
  selector:
    app: stg-subscriber-fe
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
