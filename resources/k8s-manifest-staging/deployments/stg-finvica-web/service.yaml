apiVersion: v1
kind: Service
metadata:
  name: stg-finvica-web
  namespace: stg
  labels:
    app: stg-finvica-web
    environment: stg
spec:
  type: ClusterIP
  selector:
    app: stg-finvica-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
