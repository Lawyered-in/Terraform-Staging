# k8s-manifest
This repository serves as the single source of truth for all Kubernetes resource manifests (Deployments, Services, ConfigMaps, Ingresses, etc.) that are deployed across our environments. It is designed to be connected to **ArgoCD** as a GitOps configuration repository
